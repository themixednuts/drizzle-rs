/target
.claude/
/tmp
/.idea/
/queries/
.codemogger/

# act (local GitHub Actions runner)
.env
.act-artifacts/
tmpclaude-*

# release-plz generates the config schema on demand for editor autocomplete:
#   release-plz generate-schema
.schema/
