[package]
name = "spacetime-bench-module"
version = "0.1.0"
edition = "2024"
publish = false

[workspace]

[lib]
crate-type = ["cdylib"]

[dependencies]
rand = { version = "0.9", default-features = false, features = ["std_rng"] }
spacetimedb = "2.2.0"
